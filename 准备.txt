### 第一阶段：规划与脚手架（20-30分钟）
1. ✅ 确定技术栈

## 🛠️ 技术栈规划

### 后端
| 工具 | 版本 | 用途 |
|---|---|---|
| Python | 3.10+ | 编程语言 |
| Flask | 2.x | Web 框架 |
| Flask-CORS | 4.x | 跨域处理 |
| requests | 2.x | HTTP 请求（调用天气接口） |
| python-dotenv | 1.x | 环境变量管理 |
| dashscope | 1.x | 阿里云通义千问 SDK |

### 数据库
| 工具 | 版本 | 用途 |
|---|---|---|
| SQLite | 3.x | 本地数据库（轻量、无需额外安装） |
| sqlite3 | 内置 | Python 原生数据库驱动 |

### 前端
| 工具 | 版本 | 用途 |
|---|---|---|
| HTML5 | - | 页面结构 |
| CSS3 | - | 样式布局 |
| JavaScript | ES6+ | 前端交互逻辑 |
| Fetch API | - | 接口请求 |

### 测试与工具
| 工具 | 版本 | 用途 |
|---|---|---|
| pytest | 7.x | 单元测试 |

## 📁 项目结构
```
厦门旅游/
├── app.py                    # Flask 入口
├── .env                      # 环境变量
├── .env.example              # 环境变量模板
├── requirements.txt          # 依赖清单
├── database/
│   ├── __init__.py
│   ├── schema.sql            # 数据库初始化脚本
│   └── seed_data.py          # 样例数据
├── routes/
│   ├── __init__.py
│   ├── attractions.py        # 景点接口
│   ├── weather.py            # 天气接口
│   ├── comments.py           # 评论接口
│   ├── favorites.py          # 收藏接口
│   └── routes.py             # 路线推荐接口
├── services/
│   ├── __init__.py
│   ├── weather_service.py    # 天气服务（代理+缓存）
│   └── recommendation.py     # 推荐逻辑（含LLM）
├── tests/                    # 测试文件
└── static/
    ├── index.html            # 前端页面
    ├── css/
    └── js/
```

## 🔧 环境变量配置
```
# 天气接口配置
WEATHER_API_URL=http://shanhe.kim/api/za/tianqi.php

# 大模型 API 配置
DASHSCOPE_API_KEY=sk-5fe4e4f152eb48a5991ed8265fc4feba
LLM_MODEL_NAME=qwen-plus-2025-07-28

# 数据库配置
DATABASE_PATH=./data/xiamen_travel.db

# 天气缓存时间（秒）
WEATHER_CACHE_TTL=300
```

2. ✅ 创建项目目录结构
3. ✅ 编写数据库初始化脚本
4. ✅ 准备样例数据（景点、标签）
5. ✅ 列出完整接口清单
py -c "from database import get_db_connection; conn = get_db_connection(); conn.execute('DELETE FROM weather_cache'); conn.commit(); conn.close(); print('缓存已清除')"
## 🔌 接口清单

### 景点接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/attractions` | 获取景点列表 | keyword, tag_id, area, sort_by, sort_order, page, per_page |
| GET | `/api/attractions/:id` | 获取景点详情 | id |

### 天气接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/weather/xiamen` | 获取厦门天气 | - |

### 评论接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/comments?attraction_id=:id` | 获取景点评论 | attraction_id, page, per_page |
| POST | `/api/comments` | 提交评论 | attraction_id, username, content, rating |

### 收藏接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/favorites` | 获取用户收藏 | user_id |
| POST | `/api/favorites` | 添加收藏 | attraction_id, user_id |
| DELETE | `/api/favorites/:id` | 删除收藏 | id |

### 路线接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/routes/recommend` | 获取推荐路线 | theme, duration, weather |
| POST | `/api/routes/custom` | 创建自定义路线 | name, description, theme, attraction_ids |

### 标签接口

| 方法 | 路径 | 功能 | 参数 |
|---|---|---|---|
| GET | `/api/tags` | 获取所有标签 | - |

### 响应格式

成功响应：
```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "request_id": "uuid"
}
```

错误响应：
```json
{
  "code": 400,
  "message": "error message",
  "data": null,
  "request_id": "uuid"
}
```

### 第二阶段：打通最小闭环（40-50分钟）
1. ✅ 数据库初始化并插入样例数据
2. ✅ 后端框架搭建
3. ✅ 实现 GET /api/attractions 接口
4. ✅ 前端页面基础结构
5. ✅ 前端调用接口展示景点列表
6. ✅ 验证：页面能展示数据库中的景点
### 第三阶段：接入天气与写入能力（30-40分钟）
1. ✅ 后端代理天气接口
2. ✅ 实现天气缓存（避免频繁请求）
3. ✅ 处理天气接口异常和降级
4. ✅ 实现 POST /api/comments 评论接口
5. ✅ 实现 POST /api/favorites 收藏接口
6. ✅ 前端添加评论和收藏功能
7. ✅ 验证：天气显示正常，评论和收藏能写入数据库
### 第四阶段：补齐深度要求（30-40分钟）
1. ✅ 景点筛选（关键词、区域、标签）
2. ✅ 景点排序（热度、收藏数、推荐指数）
3. ✅ 路线推荐逻辑（基于天气、偏好）
4. ✅ 分页处理
5. ✅ 错误处理（空数据、参数校验、重复提交）
6. ✅ 添加基础测试
7. ✅ 日志记录（request_id、耗时、错误）
8. ✅ 编写 README（安装、启动、测试说明）
9. ✅ 编写 AI 使用说明